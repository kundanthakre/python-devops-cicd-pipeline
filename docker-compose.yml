```yaml id="yjlwm5"
version: '3'

services:

  python-app:
    build:
      context: .
      dockerfile: docker/Dockerfile

    container_name: python-app

    ports:
      - "5000:5000"

    restart: always
```
